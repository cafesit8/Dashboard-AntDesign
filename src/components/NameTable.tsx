export default function NameTable ({ text }: { text: string }) {
  return (
    <div className="text-center w-full">
      <span className='sm:text-base font-normal text-sm font-["Outfit_Variable"]'>{text}</span>
    </div>
  )
}
